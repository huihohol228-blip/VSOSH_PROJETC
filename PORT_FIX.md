# 🔧 Исправление ошибки '$PORT' is not a valid port number

## Проблема
Railway не может раскрыть переменную `$PORT` в Dockerfile CMD.

## ✅ Решение
Исправлен Dockerfile - используется shell форма для правильного раскрытия переменной.

## Что изменено
Dockerfile теперь использует:
```dockerfile
CMD ["sh", "-c", "gunicorn --bind 0.0.0.0:${PORT:-5000} ..."]
```

Это позволяет:
- Правильно раскрыть переменную `$PORT` от Railway
- Использовать дефолтное значение 5000 если PORT не установлен

## Что делать
1. Закоммитьте изменения:
   ```bash
   git add Dockerfile
   git commit -m "Fix PORT variable in Dockerfile"
   git push
   ```

2. Railway автоматически пересоберет проект

3. Проверьте что healthcheck проходит


