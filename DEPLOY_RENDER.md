# 🚀 Развертывание на Render.com (ПРОЩЕ Railway!)

## ✅ Render.com - ЛУЧШИЙ вариант для Flask!

Render проще чем Railway и лучше работает с Python/Flask приложениями.

## 📋 Пошаговая инструкция:

### 1. Создайте аккаунт на Render

1. Зайдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub (используйте тот же аккаунт где ваш репозиторий)

### 2. Создайте новый Web Service

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Выберите ваш репозиторий: **VSOSH_PROJETC**
3. Нажмите **"Connect"**

### 3. Настройте сервис:

**Основные настройки:**
- **Name:** `phishing-detector` (или любое другое)
- **Region:** `Frankfurt` (или ближайший к вам)
- **Branch:** `main`
- **Root Directory:** (оставьте пустым)
- **Runtime:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `python -c "import os; port = int(os.environ.get('PORT', 5000)); from webapp.app_production import app; app.run(host='0.0.0.0', port=port, debug=False)"`

**Дополнительно:**
- **Plan:** Free (бесплатно)
- **Auto-Deploy:** Yes (автоматический деплой при пуше в Git)

### 4. Сохраните и дождитесь деплоя

Render автоматически:
- Установит зависимости
- Запустит приложение
- Выдаст HTTPS URL

### 5. Получите URL

После деплоя Render даст URL типа:
```
https://phishing-detector.onrender.com
```

### 6. Проверьте работу

Откройте в браузере:
```
https://phishing-detector.onrender.com/api/health
```

Должно вернуться:
```json
{"status": "ok", "model_loaded": true, "service": "phishing-detector"}
```

### 7. Обновите Telegram бота

Откройте `tgbot/webapp_bot.py` и замените:
```python
WEBAPP_URL = "https://phishing-detector.onrender.com/webapp"
```

## ✅ Преимущества Render:

- ✅ Проще настройка чем Railway
- ✅ Бесплатный план
- ✅ Автоматический HTTPS
- ✅ Автоматический деплой из Git
- ✅ Хорошо работает с Flask/Python
- ✅ Простая панель управления

## 🔍 Если что-то не работает:

1. **Проверьте логи в Render:**
   - Dashboard → Ваш сервис → Logs
   - Посмотрите ошибки при запуске

2. **Проверьте что модели в репозитории:**
   - `modelN/phishing_model_rf.pkl`
   - `modelN/phishing_model_lr.pkl`
   - `modelN/tfidf_vectorizer.pkl`

3. **Проверьте requirements.txt:**
   - Все зависимости должны быть указаны

## 🎉 Готово!

После настройки Render автоматически будет обновлять приложение при каждом `git push`!

