# 🌐 Настройка Telegram Web App (HTTPS через ngrok)

## ⚠️ Проблема

Telegram требует **HTTPS** для Web App, а `http://localhost:5000` не работает.

## ✅ Решение: ngrok

### Шаг 1: Установите ngrok

1. Скачайте: https://ngrok.com/download
2. Распакуйте `ngrok.exe` в папку проекта или добавьте в PATH

**Или через пакетный менеджер:**
```bash
# Chocolatey
choco install ngrok

# Scoop
scoop install ngrok
```

### Шаг 2: Запустите Flask API

```bash
cd webapp
python app.py
```

API должен работать на `http://localhost:5000`

### Шаг 3: Запустите ngrok

В **новом окне терминала**:

```bash
ngrok http 5000
```

Вы увидите что-то вроде:
```
Forwarding   https://abc123.ngrok.io -> http://localhost:5000
```

### Шаг 4: Скопируйте HTTPS URL

Скопируйте **HTTPS URL** из вывода ngrok (например: `https://abc123.ngrok.io`)

### Шаг 5: Обновите webapp_bot.py

Откройте `tgbot/webapp_bot.py` и замените:

```python
WEBAPP_URL = "https://your-ngrok-url.ngrok.io/webapp"  # ⚠️ ЗАМЕНИТЕ!
```

На ваш ngrok URL:

```python
WEBAPP_URL = "https://abc123.ngrok.io/webapp"  # ✅ Ваш URL
```

### Шаг 6: Запустите бота

```bash
cd tgbot
python webapp_bot.py
```

### Шаг 7: Проверьте в Telegram

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Нажмите кнопку "🛡️ Открыть проверку фишинга"
5. Web App должен открыться! 🎉

## 🚀 Автоматический запуск

Используйте `start_with_ngrok.bat` для автоматического запуска всего:

```bash
start_with_ngrok.bat
```

Или через `start.bat` выберите вариант 1.

## ⚠️ Важно

- **ngrok должен работать** пока вы используете Web App
- При перезапуске ngrok URL изменится - обновите `WEBAPP_URL`
- Для постоянного URL используйте ngrok с аккаунтом (бесплатно)

## 🔒 Постоянный URL (опционально)

1. Зарегистрируйтесь на https://ngrok.com (бесплатно)
2. Получите authtoken
3. Выполните: `ngrok config add-authtoken YOUR_TOKEN`
4. Используйте: `ngrok http 5000 --domain=your-domain.ngrok.io`

## 📝 Пример полного процесса

```bash
# Терминал 1: Flask API
cd webapp
python app.py

# Терминал 2: ngrok
ngrok http 5000
# Копируем: https://abc123.ngrok.io

# Терминал 3: Обновляем webapp_bot.py
# WEBAPP_URL = "https://abc123.ngrok.io/webapp"

# Терминал 4: Бот
cd tgbot
python webapp_bot.py
```

Готово! 🎉


