# ⚡ Быстрый старт

## 🚀 Запуск проекта локально

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Обучение моделей (если еще не обучены)

```bash
python obuch.py
```

### 3. Запуск Flask API

```bash
cd webapp
python app.py
```

API будет доступен на: `http://localhost:5000`

### 4. Запуск Telegram бота

**Полнофункциональный бот (текст, фото, .eml):**
```bash
cd tgbot
python bot_full.py
```

**Простой бот (только текст):**
```bash
cd tgbot
python bot.py
```

**Бот с Web App (требует HTTPS):**
```bash
cd tgbot
python webapp_bot.py
```

## 📱 Telegram Web App

Для работы Web App нужен HTTPS. Варианты:
1. **Railway/Render** - см. `deploy_guide.md`
2. **ngrok** - для локального тестирования
3. **cloudflared** - альтернатива ngrok

## 🌐 Развертывание в продакшен

См. `DEPLOY.md` и `deploy_guide.md` для подробных инструкций.

## 🔧 Проблемы?

- Проверьте что модели обучены: `modelN/*.pkl`
- Убедитесь что установлен Tesseract OCR (для проверки изображений)
- Проверьте токен бота в `tgbot/bot*.py`


