# Chrome расширение для проверки фишинга

Расширение для браузера Chrome, которое анализирует содержимое веб-страниц на предмет фишинга с помощью AI модели.

## Функционал

- 🔍 Автоматическое извлечение текста со страницы
- 🤖 Анализ через Flask API с использованием обученной модели
- 📊 Визуальное отображение результатов анализа
- ⚠️ Определение наличия URL, Email и телефонов
- 🎨 Современный и удобный интерфейс

## Установка

### 1. Установите зависимости для Flask сервера

```bash
cd C:\Users\gena229\Desktop\datasets\modelN
pip install -r requirements.txt
pip install flask-cors
```

### 2. Запустите Flask сервер

```bash
cd C:\Users\gena229\Desktop\datasets\modelN
python flask_example.py
```

Сервер будет доступен по адресу: `http://localhost:5000`

### 3. Установите расширение в Chrome

1. Откройте Chrome и перейдите по адресу: `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку: `C:\Users\gena229\Desktop\datasets\razshir`
5. Расширение будет установлено и появится в списке

### 4. Использование

1. Откройте любую веб-страницу
2. Нажмите на иконку расширения в панели инструментов Chrome
3. Нажмите кнопку "Проверить страницу"
4. Дождитесь результатов анализа

## Структура файлов

```
razshir/
├── manifest.json      # Конфигурация расширения
├── popup.html         # HTML интерфейс
├── popup.js           # Основная логика расширения
├── content.js         # Content script
├── styles.css         # Стили интерфейса
├── icons/             # Иконки расширения
└── README.md          # Документация
```

## Настройки

В интерфейсе расширения можно изменить URL API сервера (по умолчанию: `http://localhost:5000`).

## API Endpoints

Расширение использует следующие endpoints Flask API:

- `POST /predict` - Полный анализ текста
- `GET /health` - Проверка доступности API

## Формат запроса

```json
{
  "text": "Текст для анализа",
  "url": true,
  "email": false,
  "phone": false
}
```

## Формат ответа

```json
{
  "success": true,
  "result": {
    "phishing_percentage": 69.98,
    "is_phishing": true,
    "confidence": 69.98
  },
  "input": {
    "text": "Текст...",
    "url": true,
    "email": false,
    "phone": false
  }
}
```

## Интерпретация результатов

- **0-30%** - Безопасно (зеленый)
- **30-50%** - Подозрительно (желтый)
- **50-70%** - Опасно (оранжевый)
- **70-100%** - Критично (красный)

## Требования

- Google Chrome (версия 88+)
- Python 3.7+
- Flask сервер должен быть запущен
- Модель должна быть обучена и сохранена в папке `modelN`

## Устранение неполадок

### Расширение не работает

1. Проверьте, что Flask сервер запущен
2. Проверьте URL API в настройках расширения
3. Откройте консоль разработчика (F12) для просмотра ошибок

### Ошибка CORS

Убедитесь, что в `flask_example.py` установлен `flask-cors` и включен CORS:

```python
from flask_cors import CORS
CORS(app, resources={r"/*": {"origins": "*"}})
```

### API недоступен

1. Проверьте, что сервер запущен на порту 5000
2. Проверьте настройки файрвола
3. Убедитесь, что URL в расширении правильный

## Разработка

Для разработки и отладки:

1. Включите "Режим разработчика" в `chrome://extensions/`
2. Нажмите "Обновить" после изменения файлов
3. Используйте консоль разработчика (F12) для отладки

## Лицензия

Для внутреннего использования.

