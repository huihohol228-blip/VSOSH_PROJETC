# 🌐 Веб-приложение для проверки на фишинг

Современное веб-приложение с красивым интерфейсом для проверки материалов на фишинг.

## ✨ Функции

- 📝 **Проверка текста** - Вставьте текст для анализа
- 📷 **Проверка изображений** - Загрузите фото с текстом (OCR)
- 📧 **Проверка .eml файлов** - Анализ email писем
- 🎨 **Красивый интерфейс** - Современный дизайн с градиентами и анимациями
- 📊 **Детальный анализ** - Процентный показатель риска, найденные элементы
- 💡 **Рекомендации** - Советы по безопасности

## 🚀 Быстрый запуск

### Windows

Просто запустите:
```bash
start_webapp.bat
```

### Вручную

```bash
cd webapp
pip install -r requirements.txt
python app.py
```

Затем откройте в браузере: **http://localhost:5000**

## 📋 Требования

- Python 3.7+
- Обученные модели в папке `../modelN/`
- Tesseract OCR (для работы с изображениями, опционально)

## 🎨 Особенности интерфейса

- **Темная тема** с градиентами
- **Адаптивный дизайн** для мобильных устройств
- **Drag & Drop** для загрузки файлов
- **Анимации** и плавные переходы
- **Визуальная индикация** уровня риска:
  - 🟢 Безопасно (0-30%)
  - 🟡 Подозрительно (30-50%)
  - 🟠 Опасно (50-70%)
  - 🔴 Критично (70-100%)

## 🔧 API Endpoints

### POST `/api/predict/text`
Проверка текста

**Request:**
```json
{
  "text": "Your text here"
}
```

**Response:**
```json
{
  "success": true,
  "result": {
    "percentage": 69.98,
    "is_phishing": true,
    "confidence": 69.98,
    "risk_level": "danger",
    "risk_name": "Опасно",
    "risk_emoji": "🟠",
    "risk_color": "#fd7e14"
  },
  "found": {
    "urls": ["http://example.com"],
    "emails": ["test@example.com"],
    "phones": [],
    "url_count": 1,
    "email_count": 1,
    "phone_count": 0
  },
  "recommendation": "..."
}
```

### POST `/api/predict/image`
Проверка изображения (multipart/form-data)

**Request:** Form data с файлом `file`

### POST `/api/predict/eml`
Проверка .eml файла (multipart/form-data)

**Request:** Form data с файлом `file`

### GET `/api/health`
Проверка работоспособности

## 📁 Структура

```
webapp/
├── app.py              # Flask приложение
├── utils.py            # Вспомогательные функции
├── requirements.txt    # Зависимости
├── start_webapp.bat    # Скрипт запуска (Windows)
├── templates/          # HTML шаблоны
│   └── index.html
├── static/             # Статические файлы
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── app.js
└── uploads/            # Временные загруженные файлы
```

## 🛠️ Разработка

### Запуск в режиме отладки

```bash
python app.py
```

Приложение автоматически запустится в режиме отладки.

### Настройка порта

Измените в `app.py`:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```

## 🔐 Безопасность

- Максимальный размер файла: 16 MB
- Файлы автоматически удаляются после обработки
- Проверка типов файлов перед обработкой
- Защита от XSS через экранирование

## 📝 Примечания

- Для работы с изображениями нужен Tesseract OCR
- Модель должна быть обучена перед использованием
- Все загруженные файлы временные и удаляются автоматически

## 🌟 Скриншоты

Приложение имеет:
- Современный темный дизайн
- Градиентные кнопки и элементы
- Круговой индикатор процента риска
- Детальную статистику найденных элементов
- Красивые анимации и переходы

## 📄 Лицензия

Для внутреннего использования.

