# 🛡️ Проект проверки на фишинг

Полнофункциональная система для проверки материалов на фишинг с несколькими интерфейсами:
- 🌐 Веб-приложение (Flask)
- 📱 Telegram бот
- 📱 Telegram Web App
- 🔌 Chrome расширение
- 🔌 REST API

## 🚀 Быстрый старт

### Локальный запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск Flask API
cd webapp
python app.py
```

### Telegram бот

```bash
cd tgbot
python bot_full.py  # Полнофункциональный бот
# или
python bot.py       # Простой бот (только текст)
```

## 📁 Структура проекта

```
datasets/
├── modelN/           # Модели ML и Flask API
├── webapp/           # Веб-приложение Flask
│   ├── app.py        # Разработка
│   └── app_production.py  # Продакшен
├── web_tg/           # Telegram Web App (статические файлы)
├── tgbot/            # Telegram боты
├── razshir/          # Chrome расширение
└── obuch.py          # Обучение моделей
```

## 🌐 Развертывание

### Для Telegram Web App нужен HTTPS!

См. подробные инструкции:
- `DEPLOY.md` - общая информация о развертывании
- `deploy_guide.md` - пошаговое руководство
- `БЕЗ_NGROK.md` - варианты без ngrok

### Быстрый деплой на Railway/Render:

1. Загрузите проект в GitHub
2. Подключите к Railway.app или Render.com
3. Получите HTTPS URL
4. Обновите `tgbot/webapp_bot.py`: `WEBAPP_URL = "ваш-https-url/webapp"`

## ✨ Функции

- ✅ Проверка текста на фишинг
- ✅ Проверка изображений (OCR с Tesseract)
- ✅ Проверка .eml файлов
- ✅ ML модели (Random Forest, Logistic Regression)
- ✅ REST API для интеграции
- ✅ Telegram бот (текст, фото, файлы)
- ✅ Telegram Web App (красивый интерфейс)
- ✅ Chrome расширение

## 📦 Требования

- Python 3.7+
- Flask
- scikit-learn
- Tesseract OCR (для распознавания изображений)
- Все зависимости в `requirements.txt`

## 🔧 Установка зависимостей

```bash
pip install -r requirements.txt
```

Для OCR установите Tesseract:
- Windows: https://github.com/UB-Mannheim/tesseract/wiki
- Linux: `sudo apt install tesseract-ocr`
- macOS: `brew install tesseract`

## 📝 Обучение моделей

```bash
python obuch.py
```

Модели сохранятся в `modelN/`:
- `phishing_model_rf.pkl` - Random Forest
- `phishing_model_lr.pkl` - Logistic Regression
- `tfidf_vectorizer.pkl` - TF-IDF векторизатор

## 🎯 Использование

### API

```bash
# Проверка текста
curl -X POST http://localhost:5000/api/predict/text \
  -H "Content-Type: application/json" \
  -d '{"text": "Проверьте это сообщение"}'

# Проверка изображения
curl -X POST http://localhost:5000/api/predict/image \
  -F "file=@image.png"

# Проверка .eml файла
curl -X POST http://localhost:5000/api/predict/eml \
  -F "file=@email.eml"
```

### Telegram бот

1. Создайте бота через @BotFather
2. Получите токен
3. Обновите `BOT_TOKEN` в `tgbot/bot*.py`
4. Запустите: `python tgbot/bot_full.py`

### Chrome расширение

1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `razshir/`

## 📄 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются!

## 📧 Контакты

При возникновении проблем создайте issue в репозитории.
