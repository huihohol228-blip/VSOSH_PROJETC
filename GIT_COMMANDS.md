# 🚀 Команды для загрузки проекта в Git

## Для вашего репозитория: https://github.com/huihohol228-blip/vsos.git

### Выполните команды по порядку:

```bash
# 1. Перейдите в папку проекта
cd C:\Users\gena229\Desktop\datasets

# 2. Инициализируйте Git (если еще не инициализирован)
git init

# 3. Добавьте все файлы
git add .

# 4. Сделайте первый коммит
git commit -m "Initial commit: Phishing detection project"

# 5. Переименуйте ветку в main (если нужно)
git branch -M main

# 6. Подключите к вашему GitHub репозиторию
git remote add origin https://github.com/huihohol228-blip/vsos.git

# 7. Загрузите код на GitHub
git push -u origin main
```

## ⚠️ Если репозиторий уже был инициализирован ранее:

Если Git уже инициализирован, пропустите `git init` и выполните остальные команды.

## ⚠️ Если remote уже существует:

Если вы уже пытались подключить remote, сначала удалите его:
```bash
git remote remove origin
```

Затем выполните команды выше с шага 6.

## 🔍 Проверка перед загрузкой:

```bash
# Проверить статус файлов
git status

# Проверить что remote подключен
git remote -v

# Посмотреть что будет загружено
git ls-files
```

## ✅ После успешной загрузки:

1. Зайдите на https://github.com/huihohol228-blip/vsos
2. Убедитесь что все файлы загружены
3. Проверьте что модели `.pkl` в репозитории (важно!)

## 🆘 Если возникли проблемы:

**Ошибка: "remote origin already exists"**
```bash
git remote remove origin
git remote add origin https://github.com/huihohol228-blip/vsos.git
```

**Ошибка: "Updates were rejected"**
```bash
# Если в GitHub репозитории уже есть файлы, сначала получите их:
git pull origin main --allow-unrelated-histories
# Затем снова:
git push -u origin main
```

**Файлы слишком большие:**
- GitHub лимит: 100MB на файл
- Если модели больше - используйте Git LFS:
```bash
git lfs install
git lfs track "*.pkl"
git add .gitattributes
git commit -m "Add Git LFS tracking for .pkl files"
git push -u origin main
```

