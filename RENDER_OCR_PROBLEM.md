# ⚠️ Проблема с OCR на Render.com Free плане

## Проблема
Render.com **free план НЕ поддерживает установку системных пакетов** (таких как Tesseract OCR) через buildCommand, потому что:
- Нет доступа к `sudo` или `apt-get`
- Docker требует платный план или специальной настройки
- BuildCommand выполняется в ограниченной среде

## ✅ Решения:

### Вариант 1: Использовать Railway.app (РЕКОМЕНДУЕТСЯ) 🚀

Railway.app **БЕСПЛАТНО** и лучше поддерживает Docker:

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий `VSOSH_PROJETC`
5. Railway **автоматически** обнаружит Dockerfile и установит Tesseract!
6. Получите URL и обновите `tgbot/webapp_bot.py`

**Преимущества Railway:**
- ✅ Бесплатно (есть free tier)
- ✅ Автоматически работает с Docker
- ✅ Tesseract установится автоматически
- ✅ Проще настройка

### Вариант 2: Отключить OCR на Render (временное решение)

Если нужно срочно использовать Render, можно временно отключить OCR:
- Веб-приложение будет работать
- Текст и .eml файлы будут работать
- Только проверка изображений не будет работать

### Вариант 3: Обновить план Render (платно)

Render платные планы поддерживают Docker, но это стоит денег.

## 🎯 Рекомендация:

**Используйте Railway.app** - это самый простой и быстрый способ получить работающий OCR!

Инструкция для Railway:
1. Зайдите на https://railway.app
2. "New Project" → "Deploy from GitHub repo"  
3. Выберите репозиторий
4. Всё! Dockerfile заработает автоматически.

