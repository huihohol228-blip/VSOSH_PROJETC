# 📦 Инструкция по загрузке в Git

## ✅ Проект подготовлен для Git!

Все необходимые файлы готовы к коммиту.

## 🚀 Быстрая загрузка в GitHub

### 1. Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Название: `phishing-detector` (или любое другое)
4. НЕ добавляйте README, .gitignore, лицензию (у нас уже есть)
5. Нажмите "Create repository"

### 2. Инициализируйте Git локально

```bash
# Перейдите в папку проекта
cd C:\Users\gena229\Desktop\datasets

# Инициализируйте Git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Phishing detection project"

# Подключите к GitHub (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Загрузите на GitHub
git branch -M main
git push -u origin main
```

## 📋 Что будет загружено

### ✅ Включено:
- ✅ Весь исходный код Python
- ✅ Модели ML (`.pkl` файлы в `modelN/`)
- ✅ HTML/CSS/JS файлы
- ✅ Конфигурационные файлы (`requirements.txt`, `Dockerfile`, `Procfile`)
- ✅ Документация (`README.md`, `DEPLOY.md`)
- ✅ Telegram Web App файлы (`web_tg/`)
- ✅ Chrome расширение (`razshir/`)

### ❌ Исключено (через .gitignore):
- ❌ `__pycache__/` папки
- ❌ `.pyc` файлы
- ❌ Временные файлы
- ❌ Загруженные файлы из `webapp/uploads/`
- ❌ Виртуальные окружения
- ❌ IDE настройки

## ⚠️ Важно!

**Модели `.pkl` ДОЛЖНЫ быть в репозитории!**

Убедитесь что эти файлы есть:
- `modelN/phishing_model_rf.pkl`
- `modelN/phishing_model_lr.pkl`
- `modelN/tfidf_vectorizer.pkl`

Если их нет, сначала запустите:
```bash
python obuch.py
```

## 🔐 Конфиденциальная информация

**НЕ коммитьте:**
- Токены ботов (они уже в коде, но для продакшена используйте переменные окружения)
- Личные данные
- Секретные ключи

## 📝 После загрузки в Git

1. **Развертывание:**
   - Следуйте инструкциям в `deploy_guide.md`
   - Подключите репозиторий к Railway/Render
   - Получите HTTPS URL

2. **Обновление бота:**
   - Обновите `tgbot/webapp_bot.py` с HTTPS URL
   - Запустите бота

## 🆘 Проблемы?

Если файлы слишком большие:
- GitHub имеет лимит 100MB на файл
- Модели `.pkl` обычно меньше, но если проблема - используйте Git LFS:
  ```bash
  git lfs install
  git lfs track "*.pkl"
  ```

Готово! 🎉


