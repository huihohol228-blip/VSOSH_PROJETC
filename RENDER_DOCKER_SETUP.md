# 🔧 Настройка Docker на Render.com для OCR

## ⚠️ Проблема
Render.com не всегда автоматически обнаруживает Dockerfile. Нужно настроить вручную.

## 📋 Инструкция:

### Вариант 1: Настройка через веб-интерфейс Render

1. **Зайдите в Dashboard Render.com**
   - Откройте ваш сервис `phishing-detector`

2. **Перейдите в Settings**
   - В боковом меню нажмите **"Settings"**

3. **Найдите раздел "Build & Deploy"**
   - Прокрутите вниз до раздела "Build Command"

4. **Очистите Build Command**
   - Удалите все из поля **"Build Command"** (оставьте пустым)
   - Удалите **"Start Command"** (оставьте пустым)

5. **Сохраните изменения**
   - Нажмите **"Save Changes"**

6. **Пересоберите приложение**
   - Перейдите в раздел **"Manual Deploy"**
   - Нажмите **"Deploy latest commit"**

### Вариант 2: Если Docker не работает - используйте buildpack с установкой Tesseract

Если Docker не работает, можно использовать buildCommand с установкой через apt-get (если Render это поддерживает).

Обновите `render.yaml`:
```yaml
services:
  - type: web
    name: phishing-detector
    env: python
    plan: free
    buildCommand: |
      sudo apt-get update -qq &&
      sudo apt-get install -y -qq tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng &&
      pip install --upgrade pip &&
      pip install -r requirements.txt
    startCommand: python -c "import os; port = int(os.environ.get('PORT', 5000)); from webapp.app_production import app; app.run(host='0.0.0.0', port=port, debug=False)"
    healthCheckPath: /api/health
```

### Вариант 3: Использовать Railway.app (проще с Docker)

Railway.app лучше поддерживает Docker:
1. Зайдите на https://railway.app
2. Создайте новый проект
3. Выберите "Deploy from GitHub repo"
4. Railway автоматически обнаружит Dockerfile

### Вариант 4: Альтернатива - отключить OCR на production

Если OCR критически не нужен, можно отключить эту функцию для production или использовать альтернативные сервисы OCR через API.

## ✅ После настройки:

1. Проверьте логи деплоя
2. Убедитесь что Tesseract установлен (в логах должно быть видно установку)
3. Проверьте работу: https://vsosh-projetc.onrender.com/api/health

