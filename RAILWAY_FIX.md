# 🔧 Исправление проблем с Railway

## Проблема: Healthcheck failed

### Причины:
1. Приложение не запускается (порт неправильный)
2. Модели не загружаются (ошибка при старте)
3. Зависимости не установлены

## ✅ Исправления:

### 1. Исправлен Dockerfile
- Теперь использует переменную `$PORT` от Railway
- Добавлена обработка дефолтного порта

### 2. Исправлен app_production.py
- Модель не падает приложение если не загрузилась
- Улучшен healthcheck endpoint

### 3. Исправлен Procfile
- Правильная команда запуска с gunicorn

## 🚀 Что делать:

### 1. Закоммитьте изменения:
```bash
git add .
git commit -m "Fix Railway deployment: use PORT env variable"
git push
```

### 2. Railway автоматически пересоберет

### 3. Проверьте логи в Railway:
- Откройте проект в Railway
- Вкладка "Deployments" → выберите последний деплой
- Вкладка "Logs" - посмотрите ошибки

## 📋 Возможные ошибки в логах:

### Если "Model not found":
Убедитесь что в репозитории есть:
- `modelN/phishing_model_rf.pkl`
- `modelN/phishing_model_lr.pkl`
- `modelN/tfidf_vectorizer.pkl`

### Если "Module not found":
Проверьте `requirements.txt` - все зависимости должны быть указаны

### Если "Port already in use":
Railway автоматически использует `$PORT`, не нужно указывать вручную

## 🔍 Проверка после исправления:

1. Откройте: `https://your-app.railway.app/api/health`
2. Должно вернуться: `{"status": "ok", "model_loaded": true}`

Если все равно не работает - проверьте логи Railway для конкретной ошибки.

