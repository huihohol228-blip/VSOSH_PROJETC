# 📱 Telegram Web App

Веб-приложение для Telegram бота, которое открывается прямо в Telegram!

## 📁 Файлы

- `index.html` - главная страница
- `style.css` - стили (адаптированы под Telegram тему)
- `app.js` - логика приложения

## 🚀 Настройка

### 1. Измените API URL

Откройте `app.js` и измените:
```javascript
const API_BASE = 'https://your-api-url.com/api';
```

На URL вашего Flask API сервера (из `webapp/app.py`).

### 2. Разместите файлы

Загрузите все 3 файла на хостинг:
- GitHub Pages
- Netlify  
- Vercel
- Или любой другой статический хостинг

### 3. Получите URL

После размещения получите HTTPS URL, например:
- `https://username.github.io/phishing-checker/`
- `https://your-app.netlify.app/`

### 4. Обновите бота

В `tgbot/webapp_bot.py` укажите полученный URL:
```python
WEBAPP_URL = "https://your-webapp-url.com"
```

## ✅ Готово!

Теперь при нажатии на кнопку в боте откроется веб-приложение!

## 🧪 Локальное тестирование

Для тестирования локально используйте ngrok:

1. Запустите локальный сервер:
```bash
cd web_tg
python -m http.server 8000
```

2. Запустите ngrok:
```bash
ngrok http 8000
```

3. Используйте полученный HTTPS URL в боте

